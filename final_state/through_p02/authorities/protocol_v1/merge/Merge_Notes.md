# IHARQ Protocol v1.0 Through P02 — Merge Notes

## Scope

This is a cumulative integration record, not a new scientific audit. Primary merge sources were the frozen cumulative Protocol through P01 and the finalized P02/L2 Annex R2 plus its machine-readable companions. Supporting project authorities were used only where necessary to preserve current official terminology and avoid merge-induced semantic drift.

## Consolidation actions

- Preserved the complete P00 and P01 phase records inside the cumulative backbone.
- Integrated the complete P02 R2 phase record as Part IV, preserving Stage11/12 amendments, A0/A4 accounting, Stage18S status, failures, gates, claim ceilings and external access rules.
- Extended the cumulative run matrix, analysis contract, ablation register, amendment/rerun register, environment/reproducibility register, external-artifact register, limitation register and downstream/current-state handoff through P02.
- Added cumulative dataset/split/model tables to make the P01→P02 scientific dependency explicit without duplicating all phase-local detail.
- Merged machine-readable run/analysis/access state into phase-indexed cumulative YAML files and preserved source P02 R2 YAMLs under a provenance subdirectory.
- Preserved P01 historical forward-looking wording while adding a cumulative transition note indicating that its P02 handoff has now been consumed.
- Harmonized current official A13 wording to `Layer 9 MyoSuite/OpenSim/static-replay Embodiment Demo`; the predecessor P01 shortened wording remains explicitly traceable as a legacy inconsistency rather than being silently erased.
- Preserved provider boundaries: P01 Protocol-critical numerical artifacts remain Kaggle-governed; P02 uses private Hugging Face preferred-workspace and archival/fallback routes with separate symbolic credential classes.

## Meaning-preservation rules applied

No accepted scientific result, run ID, phase ID, split, dataset, metric, ablation identity, terminal state, amendment classification, limitation, immutable external revision or claim restriction was changed to simplify the cumulative document. Genuine duplicate project-wide prose was consolidated only in new cumulative navigation/register sections; complete phase records remain available.

## Current authority

`IHARQ-PROTOCOL-V1-CUMULATIVE-THROUGH-P02-R1` is the one current cumulative Protocol authority through P02. P03 should extend this same phase-indexed structure.

## Final presentation and release QA

- The cumulative DOCX was rebuilt from the professionally formatted P00/P01 backbone while preserving the complete finalized P02 R2 record.
- P02-specific source styles (`Heading1`/`Heading2`/`Heading3`, `BodyText`, `FirstParagraph`, `Compact`, and the source table style) were namespaced inside the cumulative DOCX so P02 tables and hierarchy render with their validated geometry without altering P00/P01 formatting.
- An inherited footer-field ordering defect was corrected to `Page X of Y`.
- Inherited P01-only DOCX/PDF core metadata was corrected to cumulative-through-P02 metadata.
- The final metadata-only re-render is pixel-identical across all 76 pages to the visually approved render.
- The PDF delivered in the package is the exact PDF emitted from the final DOCX render used for validation.
