# Phase 01 Final Document Index

This directory contains the finalized Phase-1 documentary stack carried by the Phase-1 GitHub-ready repository successor.

## Authority order

1. `implementation/` - pre-run implementation/build authority (historical intent plus exact freeze)
2. `protocol/` - frozen cumulative Protocol v1.0 through P01
3. `analysis/` - finalized cumulative Phase Analysis with embedded Layer 0
4. `layer0/` - P01-only non-authoritative convenience derivative
5. `evidence_map/` - finalized cumulative claim-to-evidence map
6. `layer10/` - finalized cumulative read-only reproducibility/presentation authority
7. `closure/` - P01 final whole-stack synchronization and P02-readiness decision

The `layer0/` document is a derivative for convenient P01-only review; the embedded Layer-0 state in the cumulative Phase Analysis remains the current claim-governance authority.

The implementation Build Book/Annex remain pre-run authority documents and are not rewritten to pretend they were authored after the accepted execution.
